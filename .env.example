# ---- Server (apps/server) ----
HOST=0.0.0.0
PORT=3000

# ---- Web (apps/web) ----
VITE_API_BASE_URL=http://localhost:3000

# ---- Database (Prisma + SQLite) ----
# Local dev works out-of-the-box via a runtime fallback (file:./dev.db).
# Copy this file to .env and override DATABASE_URL to customize.
DATABASE_URL="file:./dev.db"
